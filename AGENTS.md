# AGENTS.md

## Scope
- This workspace is currently empty.
- Add repository-specific instructions only after source files, scripts, or docs are present.

## Current Workflow
- Create and update files directly in this workspace.
- Before documenting commands here, verify them from checked-in files such as `README.md`, `package.json`, `Makefile`, `justfile`, or CI config.

## Commands
- No verified project commands yet.
- Do not add commands here unless they are confirmed from repository files or actual project usage.

## TODO
- When the repository content is added, document the actual setup, test, lint, and run commands used by the project.
- If the project has generated files, list which paths should not be edited manually.
